on:
  push:
    branches: [ cryptoHttp ]
  pull_request:
    branches: [ cryptoHttp ]
  workflow_dispatch:   # lancement manuel

jobs:
 
  lint:
    runs-on: [self-hosted]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Docker
        run: |
          docker build -t cryptoimage .
          docker compose up -d
